from app import application
from views import index, article, blog, category, type
from views import page_about, page_app, page_links, page_search
from views import account, new_post, misc

if __name__ == '__main__':
    application.run()
